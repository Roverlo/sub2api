# Sub2API 本地维护指引

本仓库包含上游代码和用户维护的定制分支。先确认当前任务绑定分支、基线与实际 diff；不能把上游状态或其他分支的定制描述成当前实现。

- 用户要求评估上游更新时，先比较实际 diff、收益和冲突，不直接升级或整体覆盖本地代码。
- 上游同步、登录/注册、订阅/兑换、引导或部署配置任务，先读 [本地定制保护](docs/LOCAL_CUSTOMIZATIONS.md)，逐项确认哪些约定已在目标分支实现；本文档不授权补做功能。
- 同一任务持续使用同一 feature 分支。共享工作区中的未知修改保持原样，用独立 worktree 隔离；不因单个问题或新一轮复审再建分支。
- Backend 改动运行受影响 Go 包测试，共享认证/服务/数据逻辑扩大覆盖；Frontend 改动运行 `pnpm typecheck` 和相关 `pnpm test:run`。先核对当前 package.json 和测试路径，不把不存在的测试当成已执行。
- 纯文档修改检查内容、引用与 `git diff --check`。提交前只暂存本任务文件并确认无凭据；推送前核实 remote 和权限。
- 部署、数据库或配置变更前完整备份并验证回滚。获准升级本服务时只操作确认归属的 Sub2API 服务，不连带重建 PostgreSQL、Redis 或其他业务；推送不构成部署授权。
